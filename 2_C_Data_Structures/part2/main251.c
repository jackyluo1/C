#include <stdio.h>  
#include <stdlib.h>
#include "mystack.h"

typedef struct node{
        int data;
        struct node* next;
}node_t;

typedef struct stack{
        int count;
        unsigned int capacity(capacity < 33);
        node_t* head;
}stack_t;

int main(){

        stack_t* mystack = malloc(sizeof(stack_t)*1);  
        
        struct stack_t* create_stack(unsigned int capacity){
	stack_t* mystack = NULL;	
	return mystack;
}

int stack_empty(stack_t* s){

	return 0;
}

int stack_full(stack_t* s){

	return 0;
}

int stack_enqueue(stack_t* s, int item){

	return -1; 
}

int stack_dequeue(stack_t* s){

        return 9999999; 
}

unsigned int stack_size(stack_t* s){

	return 0;
}

void free_stack(stack_t* s){

}

    return 0;
}







